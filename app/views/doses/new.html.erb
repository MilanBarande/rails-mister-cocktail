<%= simple_form_for [@cocktail, @dose] do |f| %>
<%= f.error_notification %>
<%= f.input :ingredient, collection: @ingredients %>
<%= f.input :description %>
<%= f.button :submit %>
<% end %>
<div class="container">  <%= link_to 'Back', cocktail_path(@cocktail) %>
</div>
